

import  random
random .randint( 1 , 1000 )



import  random
dice_roll= random . randint ( 1 , 6 )
 print (f "Вы бросили {dice_roll}" )
Вы бросили 2


import random
num = random . random ()
 print ( num )
0 . 7189476089786198


import  random
fill_percentage = random . random () * 100 
print(f "Заполнение: {fill_percentage:.2f}%" )
Заполнение: 64.01 %



Метод random.randrange(start, stop[, step])возвращает случайно выбранное число из заданного диапазона.

Параметры метода:
start- нижний предел диапазона (включительно). Это первоначальное значение, с которого может начинаться случайный выбор. Если не указано, по умолчанию принимается нуль и диапазон начинается с 0.

stop- верхний предел диапазона (не включительно). Это означает, что выбранное случайное число будет меньше этого значения. Этот параметр обязателен, и метод не будет работать без него.

step– шаг между возможными значениями. Например, если установить stepкак 2, метод будет выбирать только четные числа или числа, кратные 2, в зависимости от start. Этот параметр является необязательным и по умолчанию равняется 1, что означает, что выбор происходит из всех последовательных чисел в диапазоне


import  random 
print ( random . randrange ( 10 )) # Верхняя граница является 10 , но не включается


import  random
target = random . randrange ( 1 , 11 , 2 ) - 2 это нечетные
 print (f "Цель: {target}" )
Цель: 3


import random
cards = [ "Туз" , "Король" , "Дама" , "Валет" , "10" , " 9" , "8" , "7" , "6" ]
random .shuffle (cards)
print (f "Перемешанная колода: {cards}" )
Перемешанная колода : ['Дама' , 'Валет' , 'Туз' , '9' , '6' , 'Король' , '10' , '7' , '8']



import  random
fruits = [ 'apple' , 'banana' , 'orange' ]
 print ( random . choice (fruits))
banana



random.choices(population, weights =None, cum_weights =None, k =1)

population- последовательность список, из которой должен быть сделан выбор.
weights- опциональный список, указывающий вероятности (веса) каждого элемента в списке population. Эти весы определяют, насколько возможно, что конкретный элемент будет выбран. Длина списка weightsдолжна быть равна длине списка population.

cum_weights– тоже опциональный список кумулятивных весов. Если он указан, список weightsигнорируется. Кумулятивный вес каждого элемента определяется как сумма его веса плюс вес всех предыдущих элементов.

k: Количество элементов для выбора По умолчанию k=1.



import random
items = [ 'яблоко' , 'банан' , 'вишня' , 'дыня' ] 
chosen_item = random .choices (items, k= 1 )
 print (chosen_item)  
[ 'дыня ']



import random
numbers = [1, 2, 3, 4, 5] 
chosen_numbers = random .choices (numbers, k= 3 )
 print (chosen_numbers)
[3, 3, 5]



import random
colors = [ 'красный' , 'зеленый' , 'синий' ] 
weights = [10, 1, 1] 
chosen_color = random .choices (colors, weights, k= 1 )
 print (chosen_color)  
[ 'красный ']


Если возникает необходимость выбрать Nэлементов из списка и они при этом не повторялись, нужно использовать метод random.sample(population, k). Он возвращает список длиной kс уникальными элементами, выбранными случайно из population.


import random
participants = ['Анна', 'Богдан', 'Віктор', 'Галина', 'Дмитро', 'Олена', 'Женя', 'Зорян', 'Ігор', 'Йосип']
team = random.sample(participants, 4)
print(f"Команда: {team}")

Команда: ['Анна', 'Віктор', 'Олена', 'Галина']



random.uniform(a, b) - метод возвращает случайное действительное число N, такое что a <= N <= b.

import  random
price = random . uniform ( 50 , 100 )
 print (f "Случайная цена: {price:.2f}" )
Случайная цена :  69.64


Ключевые аспекты: методы работы со случайными величинами

random.randint(a, b): Получение целого числа из равномерного распределения в интервале между aи b включительно.

random.random(): Получение случайного числа в интервале между 0.0(включительно) и 1.0(не включительно).

random.randrange(start, stop[, step]): Получение случайного числа из заданного диапазона с возможностью указать шаг между значениями.

random.shuffle(x): Перемещение порядка элементов в списке x.

random.choice(seq): Выбор случайного элемента из последовательности seq(списка или кортежа).

random.choices(population, weights=None, cum_weights=None, k=1): Генерация случайной выборки с возможностью указать вероятности для каждого элемента и повторения в выборке.

random.sample(population, k): Получение уникальных случайных элементов из списка populationдлиной k.

random.uniform(a, b): Получение случайного действительного Nчисла a <= N <= b




