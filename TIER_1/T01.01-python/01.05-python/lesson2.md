

--------СРАВНЕНИЕ СТРОК

LOWER
string1 = "Hello World" 
string2 = "hello world" 
if  string1.lower () == string2.lower (): # привели строки к одинаковому регистру
     print ( "Строки одинаковые" ) 
else :
     print ( "Строки разные" )
# Строки одинаковые



CASEFOLD
В UTF-8 кодировании один и тот же символ можно представить несколькими кодами, например символ  'ê' можно представить кодом U+00EA, или как последовательность двух кодов U+0065 и U+0302. По этой причине сравнение одного и того же символа может вернуть  False из-за отличий в записи.
Чтобы решить эту проблему при работе с не ASCII символами для сравнения строк, их необходимо нормализовать с помощью метода  casefoldвозвращающего строку, где все символы в нижнем регистре и без неоднозначностей, когда любой символ будет иметь только одну возможную форму записи.

text = "Python Programming" 
print (text.casefold() )




В немецком языке буква "ß" (так называемый "sharp S" или "eszett") используется для обозначения специфического звука, приближенного к удвоенному "ss". Эта буква не имеет прямого эквивалента в верхнем регистре. Традиционно, когда слово, содержащее "ß", нужно написать заглавными буквами, "ß" превращается в "SS".

german_word = 'straße'  # В нижньому регістрі
search_word = 'STRASSE'  # В верхньому регістрі

# Порівняння за допомогою lower()
lower_comparison = german_word.lower() == search_word.lower()

# Порівняння за допомогою casefold()
casefold_comparison = german_word.casefold() == search_word.casefold()

print(f"Порівняння з lower(): {lower_comparison}")
print(f"Порівняння з casefold(): {casefold_comparison}")

Порівняння з lower(): False
Порівняння з casefold(): True

# В этом примере casefold()правильно считать, что строки "straße" и "STRASSE" эквивалентны





МОДУЛЬ SHUTIL
-------- РАБОТА С АРХИВАМИ
Модуль shutilв Python – это модуль стандартной библиотеки, который предоставляет ряд функций для работы с файлами и коллекциями файлов. Этот модуль может быть использован для копирования, перемещения, переименования и удаления файлов и директорий, обеспечивая высокоуровневые операции для обработки файловой системы

shutil.make_archive(base_name, format, root_dir =None, base_dir =None)
1. base_name- путь к файлу, где необходимо сохранить архив, без расширения.
2. format- формат архива, например 'zip', 'tar', 'gztar', 'bztar'или 'xztar'.
3. root_dir- директория, из которой будет создан архив. Если не указано, используется текущая директория.
4. base_dir– директория внутри архива, с которой начнется архивация.



# Создание ZIP-архива с содержанием директории 'my_folder' 
import shutil 
shutil.make_archive( 'example' , 'zip' , root_dir= 'my_folder' )


# Распаковка архива
shutil.unpack_archive(filename, extract_dir =None, format =None)
1. filename- путь к архивному файлу, который нужно распаковать.
2. extract_dir- директория, куда будет распаковано содержимое архива. Если не указано, используется текущая директория.
3. format- формат архива например, zip, tar, gztar, bztar, или xztar. Если параметр не указан, Python пытается определить формат автоматически.



SHUHIL - ДРУГИЕ ОПЕРАЦИИ:
1. shutil.copy(src, dst)копирует файл из srcв dst. Если dstэто каталог, файл будет скопирован со своим текущим именем в этот каталог.
2. shutil.copytree(src, dst)рекурсивно копирует всю директорию srcв директорию dst.
3. shutil.move(src, dst)перемещает файл или директорию srcв dst.
4. shutil.rmtree(path)рекурсивно удаляет директорию path.
5. shutil.disk_usage(path)возвращает статистику использования диска, содержащего общий объем, использованный и свободный объем для данного пути.



import shutil

# Копіюємо файл
source_file = '/path/to/source/file.txt'
destination_dir = '/path/to/destination'
shutil.copy(source_file, destination_dir)

# Копіюємо всю директорію
source_dir = '/path/to/source/directory'
destination_dir = '/path/to/destination/directory'
shutil.copytree(source_dir, destination_dir)





МОДУЛЬ PATHLIB
Два основных класса в этом модуле – это Pathи PurePath.

Когда использовать что
🔧 Path — когда ты работаешь с настоящими файлами и папками (чтение, запись, проверка существования).
📄 PurePath — когда ты только анализируешь или формируешь пути как текст, без доступа к диску (например, в тестах, логике программы, шаблонах).


| Свойство                      | `Path`           | `PurePath`              |
| ----------------------------- | ---------------- | ----------------------- |
| Работает с файловой системой? | ✅ Да            | ❌ Нет                  |
| Можно читать/записывать файл? | ✅ Да            | ❌ Нет                  |
| Проверяет существование пути? | ✅ Да            | ❌ Нет                  |
| Только разбирает строку пути  | ⚪ Частично      | ✅ Да                   |
| Пример использования          | Работа с файлами | Анализ путей как текста  |

Path — реальный путь, связанный с файловой системой
Это «живой» путь, который может взаимодействовать с файлами и папками на твоём компьютере.

PurePath — чистое представление пути (без доступа к диску)
Это «логический» путь, просто строка с удобными методами разбора,
но он ничего не знает о реальных файлах.

# PUREPATH

from pathlib import PurePath
p = PurePath("/usr/bin/simple.jpg")
print("Name:", p.name)  
print("Suffix:", p.suffix) 
print("Parent:", p.parent)
Вывод:
Name: simple.jpg
Suffix: .jpg
Parent: \usr\bin

PurePath — методы и атрибуты:
1. p.parent указывает на родительскую директорию;
2. p.name возвращает только строку с именем директории или файла, на которую указывает p;
3. p.suffix возвращает строкой расширение файла, на который указывает  p, начиная с точки;


# PATH
from pathlib import Path

p = Path("example.txt")
p.write_text("Hello, world!")
print(p.read_text()) 
print("Exists:", p.exists()) 

Hello, world!
Exists: True




--------СОЗДАНИЕ ПУТЕЙ В PATH
from pathlib import Path

# Для Unix/Linux
path_unix = Path("/usr/bin/python3")

# Для Windows
path_windows = Path("C:\Users\Username\Documents\file.txt")



1. Абсолютный путь – это полный путь к файлу или директории от корня файловой системы. Он содержит всю информацию, необходимую для нахождения файла или директории.
2. Относительный путь – это путь к файлу или директории относительно текущего рабочего каталога. 

# Перетворення відносного шляху в абсолютний
from pathlib import Path
relative_path = Path("documents/example.txt")
absolute_path = relative_path.absolute()
print(absolute_path)



WITH_NAME

Замена имени
from pathlib import Path
# Початковий шлях до файлу
original_path = Path("documents/example.txt")
# Зміна імені файлу
new_path = original_path.with_name("report.txt")
print(new_path)
documents\report.txt



WITH_SUFFIX

from pathlib import Path
# Початковий шлях до файлу
original_path = Path("documents/example.txt")
# Зміна формату файлу
new_path = original_path.with_suffix(".md")
print(new_path)



!!!!!!!!!!!!
Методы with_name и with_suffix в классе Path модуля pathlib в Python не изменяют физическое имя файла на диске. Вместо этого они используются для создания нового объекта Path, отображающего измененный путь.
Чтобы физически изменить имя файла на диске, необходимо использовать методы работы с файловой системой, например rename. Этот вызов изменит имя файла example.txtв report.txtдиректории documentsна диске.

RENAME

from pathlib import  Path
original_path = Path("documents/example.txt") 
# Создает новый объект Path с другим именем файла 
new_path = original_path.with_name("report.txt") 
original_path. rename (new_path)





-------- ЧТЕНИЕ И ЗАПИСЬ ФАЙЛОВ В МОДУЛЕ PATHLIB

read_text()
write_text()
используются для чтения и записи текстовых файлов.


read_text()
Path.read_text(encoding= None , errors= None )
1. encoding- необязательно, имя кодировки, которое используется для декодирования файла. Если не указано, используется кодировка по умолчанию.
2. errors– необязательно, инструкция, как обрабатывать ошибки декодирования.


write_text()
Path.write_text(data, encoding= None , errors= None )
1. data- строка, которую необходимо записать в файл.
2. encoding- необязательно, имя кодировки, которое используется для декодирования файла. Если не указано, используется кодировка по умолчанию.
3. errors– необязательно, инструкция, как обрабатывать ошибки декодирования.

ПАРАМЕТР ERROR:
Как видим параметр errors, в обоих методах определяет, как должны быть обработаны эти ошибки.
1. errors='strict'. Это значение по умолчанию. Если возникает ошибка декодирования, то будет исключено UnicodeDecodeError.
2. errors='ignore'. Если мы хотим игнорировать ошибки декодирования. Части текста, которые не могут быть декодированы, будут просто пропущены.
3. errors='replace'. Если пропускать мы не хотим, то заменим невозможные для декодирования символы на специальный символ замены согласно документации символ '?'.




from pathlib import Path
# Створення об'єкту Path для файлу
file_path = Path("example.txt")
# Запис тексту у файл
file_path.write_text("Привіт світ!", encoding="utf-8")


from pathlib import Path
# Створення об'єкту Path для файлу
file_path = Path("example.txt")
# Читання тексту з файлу
text = file_path.read_text(encoding="utf-8")
print(text)


from pathlib import Path
# Створення об'єкту Path для бінарного файлу
file_path = Path("example.bin")
# Бінарні дані для запису
data = b"Python is great!"
# Запис байтів у файл
file_path.write_bytes(data)


from pathlib import Path
# Створення об'єкту Path для бінарного файлу
file_path = Path("example.bin")
# Читання байтів з файлу
binary_data = file_path.read_bytes()
print(binary_data)





-------- РАБОТА С ДИРЕКТОРИЯМИ

ITERDIR()
для получения списка всех файлов и поддиректорий в указанном каталоге

from pathlib import Path
# Створення об'єкту Path для директорії
directory = Path("./picture")
# Виведення переліку всіх файлів та піддиректорій
for path in directory.iterdir():
    print(path)

picture\bot-icon.png
picture\Logo
picture\mongodb.jpg



MKDIR()
Для создания новой директории

Path.mkdir( mode =0o777, parents = False , exist_ok = False )
1. mode- права доступа к директории используются для Linux и не актуальны для Windows.
2. parents- если имеет значение True, создаст все отсутствующие родительские директории.
3. exist_ok- если имеет значение True, ошибка не будет выброшена, если директория уже существует.

Example:
from pathlib import Path 
directory = Path( '/my_directory/new_folder' ) 
directory.mkdir( parents = True , exist_ok = True )


RMDIR()
Для удаления директории

from pathlib import  Path 
directory = Path( '/my_directory/new_folder' ) 
directory.rmdir()




Модуль pathlib также предоставляет несколько методов для проверки существования и типа файловых объектов:
1. метод exists() проверяет, существует ли файл или директория.
2. метод is_dir() проверяет, является ли объект директорией.
3. метод is_file() проверяет, является ли объект файлом.


from pathlib import Path
path = Path("./picture")

# Перевірка існування
if path.exists():
    print(f"{path} існує")

# Перевірка, чи це директорія
if path.is_dir():
    print(f"{path} є директорією")

# Перевірка, чи це файл
if path.is_file():
    print(f"{path} є файлом")


picture існує
picture є директорією





-------- ПЕРЕМЕЩЕНИЕ И КОПИРОВАНИЕ ФАЙЛОВ
Для копирования файлов используется функция shutil.copy()или shutil.copy2().
Функция shutil.copy()копирует содержимое файла, но не копирует метаданные, в то время как shutil.copy2()копирует содержимое и метаданные.

SHUHIL.COPY()

import shutil
 from pathlib import  Path
# Исходный и целевой файлы 
source = Path( '/path/to/source/file.txt' ) 
destination = Path( '/path/to/destination/file.txt' ) 
# Копирование файла 
shutil. copy (source, destination)



SHUHIL.MOVE()
Для перемещения файлов

import shutil
 from pathlib import  Path
# Исходный и целевой пути 
source = Path( '/path/to/source/file.txt' ) 
destination = Path( '/path/to/destination/file.txt' ) 
# Перемещение файла 
shutil. move (source, destination)


STAT
Метод stat() возвращает информацию о файле, включая его размер.

from pathlib import Path
file_path = Path("./picture/bot-icon.png")
# Отримання розміру файла
size = file_path.stat().st_size
print(f"Розмір файла: {size} байтів")

Розмір файла: 2876 байтів


Метод stat() также дает время создания, атрибут st_ctime,и время последней модификации файла, атрибут st_mtime.

from pathlib import Path
import time
file_path = Path("./picture/bot-icon.png")

# Час створення та модифікації
creation_time = file_path.stat().st_ctime
modification_time = file_path.stat().st_mtime
print(f"Час створення: {time.ctime(creation_time)}")
print(f"Час модифікації: {time.ctime(modification_time)}")

Час створення: Fri Dec 29 04:43:16 2023
Час модифікації: Thu May 17 19:59:44 2018



UNLINK
Для удаления файла

Path.unlink( missing_ok = False )
Если параметр missing_ok  имеет значение True, то исключение не будет выброшено, если файл не существует. 
По умолчанию False - это означает, что исключение FileNotFoundError, если файл не существует.

from pathlib import Path
# Створення об'єкту Path для файлу
file_path = Path('/path/to/file.txt')
# Перевірка, чи файл існує, перш ніж видаляти
if file_path.exists():
    file_path.unlink()
    print(f'Файл {file_path} було видалено')
else:
    print(f'Файл {file_path} не існує')










